[package]
name = "cli"
version = "0.1.0"
edition = "2024"

[dependencies]
compute_core = { path = "../compute_core", features = ["native"] }
clap = { version = "4.5", features = ["derive"] }
anyhow = { workspace = true } # Need to add this as well
